<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace=".dao.XinwenDao">

    <resultMap type=".entity.Xinwen" id="XinwenMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="keywords" column="keywords" jdbcType="VARCHAR"/>
        <result property="mediaName" column="media_name" jdbcType="VARCHAR"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="url" column="url" jdbcType="VARCHAR"/>
        <result property="ctime" column="ctime" jdbcType="TIMESTAMP"/>
        <result property="catgory" column="catgory" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="XinwenMap">
        select
          id, keywords, media_name, title, url, ctime, catgory, content
        from biyesheji.xinwen
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="XinwenMap">
        select
          id, keywords, media_name, title, url, ctime, catgory, content
        from biyesheji.xinwen
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="XinwenMap">
        select
        id, keywords, media_name, title, url, ctime, catgory, content
        from biyesheji.xinwen
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="keywords != null and keywords != ''">
                and keywords = #{keywords}
            </if>
            <if test="mediaName != null and mediaName != ''">
                and media_name = #{mediaName}
            </if>
            <if test="title != null and title != ''">
                and title = #{title}
            </if>
            <if test="url != null and url != ''">
                and url = #{url}
            </if>
            <if test="ctime != null">
                and ctime = #{ctime}
            </if>
            <if test="catgory != null and catgory != ''">
                and catgory = #{catgory}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into biyesheji.xinwen(keywords, media_name, title, url, ctime, catgory, content)
        values (#{keywords}, #{mediaName}, #{title}, #{url}, #{ctime}, #{catgory}, #{content})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into biyesheji.xinwen(keywords, media_name, title, url, ctime, catgory, content)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.keywords}, #{entity.mediaName}, #{entity.title}, #{entity.url}, #{entity.ctime},
            #{entity.catgory}, #{entity.content})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into biyesheji.xinwen(keywords, media_name, title, url, ctime, catgory, content)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.keywords}, #{entity.mediaName}, #{entity.title}, #{entity.url}, #{entity.ctime},
            #{entity.catgory}, #{entity.content})
        </foreach>
        on duplicate key update
        keywords = values(keywords) , media_name = values(media_name) , title = values(title) , url = values(url) ,
        ctime = values(ctime) , catgory = values(catgory) , content = values(content)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update biyesheji.xinwen
        <set>
            <if test="keywords != null and keywords != ''">
                keywords = #{keywords},
            </if>
            <if test="mediaName != null and mediaName != ''">
                media_name = #{mediaName},
            </if>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="url != null and url != ''">
                url = #{url},
            </if>
            <if test="ctime != null">
                ctime = #{ctime},
            </if>
            <if test="catgory != null and catgory != ''">
                catgory = #{catgory},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from biyesheji.xinwen where id = #{id}
    </delete>

</mapper>